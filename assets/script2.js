

const showQuestions = ['Does your password require a capital letter?','Does your password require a lower case?' ,'Does your password require a symbol?' ,'Does your password require a number?'];

// function Questions (showQuestions) {
// }

const questions = function(showQuestions) { 
    var passLength = prompt(show);
    console.log(passLength)
    if (passlength.onclick != ''){}
};

const showQuestions = () => { 
};

generateBtn.onclick = function () {
    // const generateBtn = document.getElementById('generateBtn');
    // this.document.getElementById('generateBtn').innerHTML;
    generateBtn.addEventListener("click", showQuestions);
    console.log(generateBtn);
    console.log(showQuestions);
};

function writePassword() {
};